# DesignEverything 8.1.0 — Interactive Question Cards

**Status**: **RELEASE CANDIDATE — feature-complete, awaiting live R-spike confirmation**  
**Cut date**: not recorded — live R-spike outstanding (see Known limitations)  
**Schema**: interview-script 2.1.0

## What shipped

- Claude Code renders native `AskUserQuestion` cards for 14 static-catalog questions (2–4 choices
  each, one optionally marked recommended) and 5 grounded three-choice hint questions. A user can
  always select the host-provided Other option or type free text.
- The committed answer is always the option's derived prose (`label: description`), never the
  internal machine token — Core owns this via `deriveAnswerText` so Claude's cards and the
  AGENTS.md text fallback can never independently drift on wording (D53/D58).
- script.yaml is the single source of truth; the AGENTS.md fallback text is generated from the
  same data and the same `deriveAnswerText` function.
- The core carries an immutable committed-answer snapshot with the prompt capability so hint
  rendering does not infer answers from model memory.
- Codex plugin installation ships a dedicated text-only skill (`adapter/codex-plugin/skills/design-everything/SKILL.md`).
  It does not claim native cards or a `UserPromptSubmit` capability.
- `ConformanceMatrix.md` publishes the `interactive_choice` capability per harness: Claude Code
  native, AGENTS.md text degradation, Cursor/Antigravity/Windsurf deferred.

## Compatibility and migration

All fields are additive: scripts without `options`, `recommendation`, or `option_hints` remain
free-text, unchanged. Copy `Design/Adapters/generated/AGENTS.sample.md` to a repository-root
`AGENTS.md` when using a generic rules-only agent. `.agents/AGENTS.md` is legacy and not a
discovery path.

## Evidence and metrics — superseded by 8.2 (D59 removed the mechanism these numbers described)

**The 32/5/84% figures below described the mandatory translate-back confirmation card. Lane 8.2
(D59, [InterviewCadencePlan.md](InterviewCadencePlan.md)) removed that card entirely** — commit now
happens immediately and `translate_back` prints alongside the result, not behind a confirmation
step. The "typed messages saved by the confirmation card" metric no longer has a mechanism to
measure, so it is retired rather than recomputed. Kept below only as historical record of the 8.1
design; do not cite the 84% figure for the current build.

- 16 questions total, 16 commits (D54 — one human turn = one commit at the *individual-question*
  level; superseded at the *turn* level by D60's batching, see next section).
- 11 card-assisted questions (`CAL0, S1–S5, S7, W1–W4`), 5 free-text questions (`S0, S6, R1, S8, W5`)
  — this classification is unaffected by D59/D60 and still holds.
- ~~Baseline (pre-8.1): 32 typed messages~~ / ~~After 8.1: 5 typed messages~~ / ~~84% reduction~~ —
  retired (2026-08-16): described a card that D59 removed, not the current build.

## 8.2 turn-count metric (measured, not modeled — replaces the retired 84% figure)

Measured by [test/journey/interactive-cards-turn-count.test.ts](../../test/journey/interactive-cards-turn-count.test.ts)
walking the real canonical web AND cli journeys (16 questions each) through the real batch-aware
state machine (`commitStep` + `computeBatch`, B24b), not a hand-typed ID list or a batch-size
model.

- **Core-level (state-machine) turns: 16 → 10**, both web and cli branches — one turn = one
  `issuePromptCapability` call = one Core-computed batch (D60). **37.5% reduction** (`Math.round`
  → 38%) in state-machine turn count.
- Batch shape (identical core prefix, branch-specific tail): `[CAL0,S0] [S1] [S2] [S3] [S4] [S5]
  [S6,S7] [R1,S8] [W1,W2,W3,W4] [W5]` for web; `[C1,C2,C3,C4] [C5]` replaces the last two web
  batches for cli.
- **This is a Core-level count, not yet confirmed to equal real UI-level turns in Claude Code.**
  Committing more than one question in a batch requires the agent to call `status --json` between
  commits *without* a fresh `UserPromptSubmit` — that assumption is exactly what the still-open
  R-spike below tests. Until R-spike closes, treat "16 → 10, 38%" as a Core-mechanics measurement,
  not a user-facing turn-count claim.

## Known limitations at RC

- **R-spike not yet run live — the one thing blocking GA, now for both 8.1's card mechanism AND
  8.2's batching.** Whether Claude Code's `UserPromptSubmit` hook fires when a user answers via a
  card (vs. types free text) has not been confirmed in a real session. 8.1's code implements
  "commit within the same turn, no cross-turn retention" as its working assumption; 8.2's batching
  additionally assumes an agent can call `status --json` mid-batch without a fresh
  `UserPromptSubmit`. If a live run disproves either, both need a follow-up patch. The probe is
  already built and ready to run — `E:\rspike-8.1-probe`, installed for real via `install.mjs` (see
  [r-spike-userpromptsubmit-probe.md](../ContractForAI/Core/v7-expansion/r-spike-userpromptsubmit-probe.md) §7
  for exact steps). This is the only reason this note is not cut GA yet.
- `test/regression/golden-web.test.ts` checks emitted doc structure against hardcoded answers fed
  straight into `emitTree()`; it never calls `commitStep`, so it has no concept of a "turn" to
  count. Turn measurement lives in the separate journey test above, not an extension of golden-web.
- Codex/AGENTS.md receive the same option catalog as text only — no native card surface, and no
  `checkRate`/token mechanism enforcing batch size (best-effort prose only); see
  `ConformanceMatrix.md`'s `interactive_choice` column, which publishes this gap rather than
  promising parity (D37).
- The 38% figure is measured on the canonical non-Other, non-timeout path only, and only at the
  Core/state-machine level (see caveat above) — not yet a confirmed real-session number.
