# Decision Log — Nhật ký quyết định

> Mỗi dòng = một quyết định + **lý do** + ngày. Để 3 tháng sau không cãi lại với chính mình. Quyết định mới ghi vào đây; quyết định bị đảo thì đánh dấu *superseded*, không xoá.

## Tại sao cần file này
Quyết định nằm rải rác trong nhiều file → quên mất *vì sao* đã chọn → dễ đảo ngược lãng phí. Log tập trung giữ lý do ở một chỗ tra cứu được.

| ID | Ngày | Quyết định | Lý do | Trạng thái |
|---|---|---|---|---|
| D1 | 2026-06-25 | Đối tượng = dự án greenfield + người **không biết viết doc** | Thu hút đông người dùng; tạo khe hở so với đối thủ (vốn giả định user đã biết mình muốn gì) | Active |
| D2 | 2026-06-25 | Cốt lõi là **PHỎNG VẤN**, không sinh từ template trống | Người mới không biết điền gì vào template rỗng | Active |
| D3 | 2026-06-25 | Hình hài = **plugin native từng harness, dùng chung một lõi** | Trải nghiệm tốt nhất + ép mạnh nhất mỗi nơi; đổi lại rủi ro bảo trì N nền tảng → kỷ luật lõi béo/adapter gầy | Active |
| D4 | 2026-06-25 | **Không build agent** — chạy trên lưng agent có sẵn | Kịch bản phỏng vấn mới là sản phẩm; không nuôi model | Active |
| D5 | 2026-06-25 | Adapter theo **HARNESS**, không theo **MODEL** | DeepSeek/GLM là model chạy trong harness → phủ harness là phủ luôn model | Active |
| D6 | 2026-06-25 | Ra mắt **Claude Code trước**, rồi `AGENTS.md` | Claude Code ép cứng được → chứng minh tầm nhìn đầy đủ, làm reference cho hợp đồng | Active |
| D7 | 2026-06-25 | Anchor neo theo **symbol name**, không line-range | Bền khi thêm/bớt dòng; line-range giòn tới mức vô dụng khi maintain | Active |
| D8 | 2026-06-25 | Anchor `rev` = **git blame SHA** | Cho biết commit nào chạm symbol → so SHA biết có drift + nối thẳng vào diff; checksum chỉ trả lời có/không | Active |
| D9 | 2026-06-25 | Scaffolding chưa có code → anchor **trỏ file dự kiến** + `status=planned` | Mỏ neo liền mạch doc→ý định→code, không quãng đứt | Active |
| D10 | 2026-06-25 | TechStack = **Node.js + TypeScript** | Cùng hệ ReportSupporter, type-safe, parse YAML dễ, hợp khi lên CLI/bot | Active |
| D11 | 2026-06-25 | `progress.json` lưu `last_user_turn_id` và chỉ cho advance tối đa 1 bước mỗi lượt người thật | Chốt enforcement "UserPromptSubmit mới được tiến bước", ngăn AI hoặc hook lặp đẩy state nhảy cóc | Active |
| D12 | 2026-06-25 | `interview-script.gate` là field **singular** trỏ 1 gate id; `Stop` không phải block action hợp lệ của `gate-policy` 0.1.0 | Giữ schema đơn giản cho MVP, tránh adapter hiểu gate khác nhau và tránh lạm dụng `Stop` thay `PreToolUse` | Active |
| D13 | 2026-06-25 | Taxonomy đầu ra gộp các quyết định auth, permissions và monetization vào `05-architecture.md` và `07-release.md` thay vì tách file riêng | Giữ bộ docs tối giản cho người mới, giảm số file phải đọc, nhưng vẫn đủ chỗ để neo các quyết định kỹ thuật và phát hành quan trọng | Active |
| D14 | 2026-06-26 | **Mô hình hai lớp** cho enforcement: lớp **skill/LLM** là actor duy nhất diễn giải ý nghĩa (commit `answered`, set `branch` sau S6, xác nhận dịch ngược); lớp **hook deterministic** chỉ ép gate-artifact (`PreToolUse`) + giới hạn nhịp một-bước-mỗi-lượt (`UserPromptSubmit`, đo bằng field mới `answered_len_at_last_turn`). `branch` do skill set và là một chiều. | Vá lỗ hổng: spec cũ bắt hook tự chọn web/mobile và chấm "câu trả lời hợp lệ" — hook không đọc được ý định (FirstIdea §6, gate-policy §1). Tách trách nhiệm giữ enforcement trung thực và buildable. Amend state-schema 0.1.0 tại chỗ vì chưa có code tiêu thụ. | Active |
| D15 | 2026-06-26 | Dữ liệu gate runtime tách thành file `Design/Content/interview-script/gate-policy.yaml` (cạnh `script.yaml`); `gate-policy.md` giữ vai hợp đồng/định nghĩa field. | Runtime không được parse markdown để lấy gate; đặt cạnh `script.yaml` để loader đọc cùng chỗ. Hai bản phải khớp, lệch thì dừng. | Active |
| D16 | 2026-06-28 | Cấu trúc gợi ý mặc định S3 (MoSCoW) thành danh sách và làm rõ cảnh báo M2 (Sync)/M5 (Store) | Phản hồi thực tế từ các phiên dogfood (proj-01, proj-02, proj-03) chỉ ra người dùng dễ bị vướng nếu gợi ý dồn cục, và cảnh báo thiếu chỉ dẫn xác nhận hoặc thiếu số phí cụ thể ($99/$25) | Active |
| D17 | 2026-06-28 | Hoãn mở rộng taxonomy nâng cao (ADR/Test-Plan/ContractForAI) | Dữ liệu dogfood 3 dự án chứng minh bản tối giản hiện tại là đủ; mở rộng sẽ gây gánh nặng nhận thức. Xem chi tiết căn cứ và điều kiện kích hoạt lại tại [taxonomy-decision.md](RoadMap/Month3/taxonomy-decision.md) | Active |
| D18 | 2026-06-29 | Hỗ trợ cấu hình tùy chọn tiền tố mỏ neo (`srcPrefix`) trong `emitTree` | Sửa lỗi F-04 khi tiền tố `apps/mobile/src/` bị ép cứng cho nhánh mobile, tạo điều kiện cấu hình cho các dự án Expo standalone mà không làm vỡ các adapter cũ | Active |
| D19 | 2026-06-29 | Bổ sung nhánh `'hybrid'` tích hợp Web & Mobile đồng thời | Giải quyết lỗi ma sát F-07 (lệch so với taxonomy-decision §5), sinh ra cả `07-deployment.md` và `07-release.md` cho dự án Hybrid mà không làm phình to taxonomy mặc định | Active |
| D20 | 2026-06-29 | Quy chuẩn quy trình vá lỗi: pain-rank Action Plan trong tương lai bắt buộc gắn nhãn Tầng | Rút kinh nghiệm từ lỗi F-04/F-07 bị trượt do phân nhầm tuần (phân vá lỗi ở tuần chỉ cho phép sửa Content). Việc gắn nhãn Tầng (Content/Taxonomy/Lõi/Adapter) giúp phân bổ công việc đúng tuần, đúng năng lực thực thi | Active |
| D21 | 2026-06-29 | Mở `branch` từ enum đóng (`core/web/mobile/hybrid`) thành tập **hình-hài dự án (project-shape) mở**; thêm shape `cli`/`tool` đầu tiên, mỗi shape có bộ câu nhánh + biến thể `07-*` riêng. **Mốc MAJOR v2.0.0.** | Kịch bản chỉ 2 nhánh web/mobile ép sai mọi dự án CLI/extension/library/desktop — đây là lý do thật sản phẩm chưa ra ngoài được. Phủ thêm hình-hài phục vụ chính đối tượng người-mới. Mở rộng [D13], KHÔNG đảo. Chi tiết + batch: [V2-ExpansionPlan.md](RoadMap/V2-ExpansionPlan.md) | Active |
| D22 | 2026-06-29 | Tách **bộ chọn hình-hài dự án** khỏi S6 thành một câu core riêng; `branch` do câu này set (một chiều). | S6 đang gánh 4 việc (team/deadline/budget/chọn nhánh), quyết định nhánh bị chôn — thêm >2 shape sẽ vỡ. Giữ `branch` một chiều theo [D14]. | Active |
| D23 | 2026-06-29 | Thêm loại câu hỏi `meta` (calibrate "giải thích kỹ vs đi nhanh"), **không neo doc** (`target_doc: null` khi `kind: meta`); chỉ set chế độ giải thích/độ push-back. | Cần đổi độ sâu giải thích theo trình độ người dùng, nhưng KHÔNG hỏi ngành học/lý lịch (không neo doc nào, dễ thành khảo sát/gác cổng). Ngoại lệ có kiểm soát của luật vàng "mỗi câu neo 1 doc". | Active |
| D24 | 2026-06-29 | **Ranh giới multi-agent:** KHÔNG dùng multi-agent cho generation; critic = **một role/pass phản biện trong agent sẵn có**, ép mạnh ở harness có hook, mềm ở rules-only. | Bài toán của sản phẩm là *moi ý định + ràng buộc* (convergent), không phải *sinh sản lượng* (divergent như tool viết truyện nhiều agent) — swarm chỉ sinh thêm doc-nghe-hợp-lý-nhưng-sai. Giữ nguyên [D4] (không build agent). | Active |
| D25 | 2026-06-29 | Phân biệt rõ: mở **hình-hài dự án** (D21) ≠ mở **doc enterprise** (ADR/test-plan/ContractForAI). [D17] vẫn Active. | Chống lẫn "mở rộng tốt" (phủ thêm loại dự án cho người mới) với "mở rộng đã hoãn" (file enterprise cho cùng một dự án) — hai thứ khác bản chất. | Active |
| D26 | 2026-06-29 | **Thứ tự Phase 2:** làm v2.0.0 (overhaul kịch bản) TRƯỚC, đóng gói/ship ra ngoài SAU. | Sản phẩm chưa đủ shape để mời người ngoài; ship sớm sẽ lấy tín hiệu sai. Đánh đổi: lùi mốc "Đáng chia sẻ". Month 4 W13D/W14A/W14B/W16 hoãn tới sau v2 (deferred post-v2, không phải BLOCKED chờ tuần). | Active |
| D27 | 2026-07-09 | Chốt vị trí và định dạng của registry hình-hài tại `shapes.yaml` | Runtime registry để engine load các hình-hài dự án mở rộng, tránh hardcode danh sách shape trong code. File đặt tại `Design/Content/interview-script/shapes.yaml`, mirror 100% mục lục `taxonomy.md` (theo tiền lệ D15). | Active |
| D28 | 2026-07-10 | Thêm **file dẫn xuất** `08-build-plan.md` vào taxonomy (mọi hình-hài): kế hoạch build dạng milestone có thứ tự, mở đầu bằng "khung xương biết đi" (walking skeleton), mỗi milestone kèm tiêu chí done-when. **KHÔNG thêm câu hỏi mới** — nội dung suy từ S3 (Must) + S5 (flow chính); slot do lớp skill điền lúc emit (`emit --slots-file`), engine giữ fallback deterministic từ answers thô. **Mốc MAJOR v3.0.0.** | Bằng chứng thực nghiệm từ smoke run W14A đầu tiên (2026-07-10, dự án yt-cli, phiên Claude Code thật): docs 00–07 trả lời tốt "xây gì/cho ai/vì sao/hướng nào" nhưng người mới vẫn không biết "bắt đầu từ đâu, thứ tự nào, xong kiểm thế nào" — chính tác giả cũng build DesignEverything bằng tầng roadmap/contract nằm dưới docs nền móng. Đây là **cầu nối docs→code cho người mới**, KHÔNG đảo [D17]/[D25] (không phải ADR/test-plan/ContractForAI enterprise). File dẫn xuất là ngoại lệ có kiểm soát thứ hai của luật "mỗi câu neo 1 doc" (sau `meta` [D23]): doc không có câu hỏi riêng, neo ngược về S3+S5. | Active |

| D29 | 2026-07-13 | Phân biệt rõ docs-emitted, plan-validated và executing; không gọi phase là ready-to-build chỉ vì các file có mặt. | Gate hiện chỉ chứng minh artifact tồn tại, không chứng minh docs đúng shape, Must có flow/task/test, Won't không bị kéo vào MVP hay rủi ro đã được nhìn thấy. Đây là lỗ hổng lộ rõ ở yt-cli. Chi tiết: [V3-ExecutionExpansionPlan.md](RoadMap/V3-ExecutionExpansionPlan.md). | Active |
| D30 | 2026-07-13 | Execution dùng một task graph máy-đọc được + execution state/evidence append-only; vẫn không dùng multi-agent generation. | Sau 08, agent vẫn tự đoán micro-decision. Một active task, precondition, allowed paths, verify và evidence tạo safety rail cho newbie mà không biến sản phẩm thành swarm hay issue tracker. Mở rộng D24, không đảo D4. | Active |
| D31 | 2026-07-13 | Gate sau planning phải mở theo task/milestone và evidence, không mở toàn bộ source tree sau emit. | Mở tất cả code sau một gate duy nhất cho phép agent nhảy M0→M4, sửa ngoài phạm vi và báo hoàn thành không có bằng chứng. Harness A ép deterministic; B luôn ghi rõ soft enforcement. | Active |
| D32 | 2026-07-13 | Mọi dependency ngoài, platform, chi phí, quyền, điều khoản hoặc giả định chưa chắc phải được ghi risk và chạy feasibility spike trước implementation liên quan. Default ưu tiên first supported environment, không tự chọn cross-platform/production. | Yt-cli chốt yt-dlp, mpv, đa OS và phân phối trước khi có proof trên máy thật. Hệ thống phải biến điều chưa biết thành spike có exit criterion, không trình bày như fact hay tư vấn pháp lý. | Active |
| D33 | 2026-07-13 | Chế độ deep/fast chỉ thay đổi độ giải thích; semantic validation, risk acknowledgement, task scope và evidence là bất biến. | Newbie cần lý do/cách đọc lỗi; expert cần nhịp nhanh. Cả hai vẫn cần cùng safety rails để không biến fast thành bypass. Mở rộng D23. | Active |
| D34 | 2026-07-13 | Claim “newbie tự vibe-code chỉn chu” chỉ được nâng sau evaluation journey thật; golden/unit test không đủ. | Test hiện tại chủ yếu chứng minh state, headings và anchors. Cần transcript có bất định, semantic mutation, adapter E2E và pilot người ngoài tác giả trước khi marketing end-to-end. | Active |
| D35 | 2026-07-13 | Lane tên V3 Execution Expansion nối tiếp 3.0.0 nhưng target release là 4.0.0: thêm 09-execution-plan, execution state và semantic/evidence gate là MAJOR. | Không được dùng tên lane để lách SemVer. 3.0.0 đã đổi taxonomy bằng 08; thay đổi public contract tiếp theo phải bump major và đồng bộ Versioning + ConformanceMatrix. | Active |
| D36 | 2026-07-13 | ExecutionPlanV3, validation digest, state và evidence thu từ runner là nguồn sự thật; thiếu/hỏng state hoặc evidence tự khai phải fail-closed. | Review B7–B10 cho thấy legacy conversion, state parse lỗi và `--exit-code 0` làm pass giả. Safety rail cho newbie vô nghĩa nếu agent tự đổi truth source. | Active |
| D37 | 2026-07-13 | Dùng `PreActionGate` trung lập runtime ở core; từng adapter công bố tool/event coverage và enforcement level. Codex adapter dùng `PreToolUse` nơi official capability hỗ trợ, không suy diễn thành sandbox toàn diện. | Giữ D3/D5 (lõi chung, adapter theo harness), đồng thời tránh gọi mọi hook là hard gate. Codex docs xác nhận PreToolUse chỉ intercept một tập tool path hữu hạn. | Active |
| D38 | 2026-07-13 | Execution plan từ folder trống phải được sinh từ `ProjectProfile` và environment doctor; không hardcode Node/npm, source path hay package scripts. | Newbie không có manifest/config để đáp ứng lệnh emitter tự bịa. Profile là điều kiện để plan vừa có thể thực hiện vừa giải thích được. | Active |
| D39 | 2026-07-13 | Newbie workflow hiển thị một next step cùng lý do/proof/recovery; mọi đổi scope, dependency hay stack đi qua amendment có diff và revalidation. | “Tự code” không phải giấu lỗi; người mới cần biết chọn gì khi preflight hoặc verify fail mà không bị agent lách qua. | Active |
| D40 | 2026-07-13 | Claim hỗ trợ newbie/cross-runtime chỉ mở sau replay có artifact và pilot ẩn danh audit được. | B10 report không có raw findings đủ để tái kiểm. Mở rộng D34 từ một journey sang mọi adapter và stack đã claim. | Active |
| D41 | 2026-07-14 | Sau M3-skeleton, mỗi Must trong `02-scope.md` sinh một feature-milestone (`M4-{feature}`…) với ≥1 hợp đồng-task build feature thật; trace-link phải trỏ task build feature, validator từ chối link Must→skeleton. Chi tiết: [V5-ContractSynthesisPlan.md](RoadMap/V5-ContractSynthesisPlan.md). | Bản V3 map mọi Must về cùng 4 task skeleton (traceability ở mép); skeleton không build feature nào. Người mới cần đi hết ≥1 Must thật, không chỉ shell chạy được. Mở rộng D30, không đảo D4. | Active |
| D42 | 2026-07-14 | Số lượng và độ nhỏ hợp đồng là hàm deterministic của quy mô (Must × entity/quan hệ `03-data-model` × số bước `04-flows`), không cố định; mỗi hợp đồng ≤~200 dòng/một tầng, vượt ngưỡng thì tự tách nhóm. | Dự án nhỏ ít hợp đồng, lớn nhiều — granularity phải giải thích được thay vì số áng chừng. Giữ luật một-đơn-vị-triển-khai của VibeCode Step 3. | Active |
| D43 | 2026-07-14 | Hợp đồng là schema máy đọc — 7 mục của [CONTRACT_STRUCTURE_RULE](ContractForAI/CONTRACT_STRUCTURE_RULE.md) thành field, tái dùng gate/evidence V3, không phải markdown tự do. | Muốn validator/gate/evidence dùng lại cơ chế V3 thì contract phải có shape kiểm được, không để executor tự chế interface. | Active |
| D44 | 2026-07-14 | Mỗi hợp đồng sinh ra bind vào một lớp Conventions của dự án đích (tech-stack pin, coding standard, test tiers, allowed-path) do tool emit từ `05-architecture`/`06-constraints`; hợp đồng không được chế stack/dep ngoài Conventions. | Sản phẩm hóa luật "cấm import lậu" của [TechStack.md](Conventions/TechStack.md) sang dự án đích, giữ tính nhất quán khi executor yếu. | Active |
| D45 | 2026-07-14 | Thêm phase runtime `reviewing` giữa `verified` và feature kế: verify xong → manager-check sinh break-task (`fix_*`/`polish_*`) nếu output bẩn; feature chưa "done" tới khi review đóng. Fail-closed. | Vòng `week{N}_break` là cơ chế chất lượng cốt lõi của dự án tham chiếu; chống "done giả". Mở rộng D31/D36. | Active |
| D46 | 2026-07-14 | Vai manager/executor map vào deep/fast (bất biến D33): deep viết+review hợp đồng, fast thực thi; không đổi kỷ luật validation/task/evidence. | Người mới cần lý do khi viết/duyệt hợp đồng; nhịp nhanh không được thành bypass validation. | Active |
| D47 | 2026-07-14 | Chất lượng hợp đồng auto không dựa vào bộ sinh mà vào ba lưới chồng nhau (grounding docs khoá + bind Conventions + review/break-task); claim "hỗ trợ build tới sản phẩm" chỉ mở sau pilot đi hết ≥1 feature Must thật trên nhiều quy mô. | Bản auto không thể hay bằng contract tay của manager giỏi; phải bọc bằng ràng buộc + review thay vì tin generator. Mở rộng D34/D40. | Active |
| D48 | 2026-07-25 | **Mốc RELEASE TRUTH SYNC v7.0.0 (v1-fix-bugs stability & parity):** đồng bộ schema, runtime test, fault injection, parity giữa Claude Code và Codex, đánh giá newbie journey, và báo cáo release công khai. Loại bỏ hoàn toàn link tuyệt đối `file:///e:/...`. **Mốc MAJOR v7.0.0** — cắt 2026-08-10 (xem [v7-release-note.md](RoadMap/v7-release-note.md), Status GA, D56 cho điều kiện đóng thật). Sửa 2026-07-30: dòng này từng ghi "khóa đồng bộ 100%" và "Active" như đã xong — sai, đúng finding R15; nay đã cắt thật với gap công khai ở §5 của release note, không phải claim "khoá đồng bộ 100%". | Đảm bảo tính trung thực của tài liệu release, bảo vệ runtime khỏi lỗi đĩa đầy, process crash, orphaned lock và khẳng định sai sự thật trong tài liệu onboarding. Xem [b5d_docs_version_release_truth_sync_contract.md](ContractForAI/Core/v1-fix-bugs/B5/b5d_docs_version_release_truth_sync_contract.md). | Active (GA 2026-08-10) |
| D53 | 2026-08-01 | Kịch bản phỏng vấn được khai `options` **máy đọc được** cho từng câu. `options` là **dữ liệu Lõi** nằm trong `script.yaml`; *cách render* (thẻ tương tác hay liệt kê text) là việc của Adapter. Xem [InteractiveQuestionCardsPlan.md](RoadMap/InteractiveQuestionCardsPlan.md). | Giữ D3/D5 "lõi béo, adapter gầy" khi thêm một bề mặt tương tác mà chỉ Claude Code có. Nếu để adapter tự chế lựa chọn thì mỗi harness sẽ hỏi một kiểu — vi phạm `Contract.md`. | Active |
| D54 | 2026-08-01 | Một lượt người thật vẫn = đúng **một** `commit`. Thẻ tương tác chỉ đổi *cách thu* câu trả lời, **không** đổi mô hình uỷ quyền: capability token (`turnCapability.ts`) và `checkRate` (`advanceState.ts:174`) giữ nguyên. Xem [InteractiveQuestionCardsPlan.md](RoadMap/InteractiveQuestionCardsPlan.md). | Lớp chống "agent tự trả lời thay rồi commit hàng loạt". Gom nhiều câu vào một lượt là lane riêng, không được lén gộp vào đây. | Superseded by D60 |
| D55 | 2026-08-01 | Mọi câu có `options` **bắt buộc** còn đường tự nhập. `options` **không thay thế** `default`: giá trị `default` phải xuất hiện như một lựa chọn mang nhãn khuyến nghị. Xem [InteractiveQuestionCardsPlan.md](RoadMap/InteractiveQuestionCardsPlan.md). | D2 là phỏng vấn, không phải form. Ép người dùng chọn trong 4 ô sẽ bóp méo câu trả lời và sinh doc sai — thà để họ gõ còn hơn nhận một lựa chọn gần đúng. | Active |
| D56 | 2026-08-10 | **Hạ điều 6/10 của DoD `plan-v1-fix.md` §10:** chấp nhận `APPROVED + IMPLEMENTED + SEAM_PARTIAL/UNIT_ONLY` (có gap ghi rõ trong §7 từng contract) thay vì đòi tuyệt đối `VERIFIED` cho cả 24 contract trước khi cắt 7.0.0; R14 (hai reviewer độc lập) và A3 (dogfood — quota provider) hạ thành limitation công bố, loại khỏi mẫu số coverage. Chi tiết: [plan-v1-fix.md §10, D56](ContractForAI/Core/v1-fix-bugs/plan-v1-fix.md). | Hai mục không bao giờ tự đóng được bằng thêm test (bằng chứng không sinh ra được / quota bên thứ ba nằm ngoài tầm kiểm soát); phần còn lại là gap hẹp đã ghi rõ, không phải "chưa biết". Cùng công thức đã dùng cho B18a/R21/R07/R16 — hạ chi phí thu thập bằng chứng, không hạ phạm vi enforcement (ranh giới giữ nguyên từ quyết định Linux gate 2026-08-03). | Active |
| D57 | 2026-08-10 | **Bỏ "pilot B18a xong" khỏi điều kiện mở lane `v6-expansion`** (điều kiện gốc ghi trong `V6-DetailedDesignPlan.md`/`v6-expansion/README.md`). Pilot B18a (kể cả bản đã hạ quy mô self-pilot 2026-08-02) **CHƯA CHẠY** — bảng kết quả `v5-feature-pilot-protocol.md` vẫn để trống, không giả lập số liệu. Đây là bằng chứng cần **hành động sống thật** (agent build trọn 1 feature) không thể tạo ra trong một phiên đối chiếu tài liệu. Mechanics-level evidence đã có qua `test/replay/featureJourneyReplay.test.ts` (mock-free, xác nhận cơ chế synth→compile→validate→review→feature-done đúng) — điều kiện cần, không phải điều kiện đủ. **Không đổi bất cứ gì về D40/D47**: claim "hỗ trợ build tới sản phẩm" vẫn bị khoá đúng như cũ, chờ pilot thật. Chỉ gỡ pilot khỏi *gate mở lane V6* — vì nội dung V6 (deepen tier-2) không phụ thuộc cơ chế feature-journey mà B18a kiểm chứng. | Cùng công thức D56/R14/A3: hạ *gate cần hành động không tự động hoá được trong phiên này*, không hạ *claim đã khoá*. Pilot B18a vẫn là việc cần làm, chỉ không còn là điều kiện tiên quyết để mở một lane không phụ thuộc nó. | Active |
| D58 | 2026-08-16 | **Thẻ tương tác commit văn xuôi, không commit token.** Khi người dùng bấm một `options` entry, `commit --answer` mang chuỗi `` `${label}: ${description}` `` (hàm `deriveAnswerText` ở Core), không phải `value`. `value` chỉ giữ vai trò khoá máy cho `--branch` (S7) và `--calibrate` (CAL0) — hai nơi engine cần một token rời rạc, không phải văn xuôi tự do. Xem [InteractiveQuestionCardsPlan.md](RoadMap/InteractiveQuestionCardsPlan.md), contract B22c/B22d. | `answers['W1']` chảy thẳng vào slot `client_and_rendering_strategy` của `05-architecture.md` ([emit.ts](../src/core/emit.ts)); W3/C5 mỗi câu nuôi 3 slot. Commit token thô sẽ ghi `public-seo` vào doc kiến trúc thay vì một câu văn đọc được — vi phạm chất lượng doc mà toàn bộ phương pháp phỏng vấn nhắm tới (D2). Kéo theo: B22b bỏ luật máy-check cũ "`options[].recommended` khớp `value === default`" (bất khả thi vì `default` là văn xuôi) để đổi bằng shape `recommendation: {mode: fixed\|contextual}`; B22e dựng lưới `warning_rules` thay thế. | Active |
| D59 | 2026-08-16 | **Bỏ thẻ xác nhận dịch ngược làm điều kiện tiên quyết để commit.** Bản dịch ngược (`translate_back`) vẫn **in ra** ngay cùng kết quả commit, cho người đọc xác nhận sau — không còn là thẻ `AskUserQuestion` thứ hai chặn trước commit. Bù lại bằng lệnh `undo` mới: hoàn tác đúng câu vừa commit gần nhất, xoá `answers`/`slots` liên quan, trả `current_step` về đúng câu đó. Xem [InterviewCadencePlan.md](RoadMap/InterviewCadencePlan.md) B24a/B24d. | 14/16 câu trong hành trình web canonical không có Critic-pass phía sau — thẻ xác nhận dịch ngược trên các câu đó là thuế đều tay (một lượt chờ xác nhận cho mọi câu), không phải lưới đỡ có mục tiêu. Lưới thật là `answer_contract` + `warning_rules` (engine ép tại thời điểm commit, không đổi) cộng `undo` (sửa được sau, không cần xác nhận trước). Đảo bốn quy tắc vàng ở SKILL.md và render-inject.ts (quy tắc 3 cũ: "dịch ngược rồi mới commit"). | Active |
| D60 | 2026-08-16 | **Gom nhiều câu vào một lượt — do Core quyết định batch, không phải agent chọn.** Token capability mang danh sách `question_ids` (thay vì một `question_id`); `checkRate` nới trần đúng bằng số câu trong batch đang phát (`turnCapabilityRecordSchema`, `computeBatch`). Agent nhận đúng batch Core đã tính, không được tự ý gộp thêm câu ngoài batch. Xem [InterviewCadencePlan.md](RoadMap/InterviewCadencePlan.md) B24b — đây chính là lane bảo lưu ở [InteractiveQuestionCardsPlan.md §7](RoadMap/InteractiveQuestionCardsPlan.md). **Supersedes D54**: "một lượt = một commit" không còn đúng nghĩa đen, nhưng tính chất chống bypass D54 nhắm tới (agent không được tự quyết định gộp bao nhiêu câu rồi commit hàng loạt) giữ nguyên — chuyển từ "mỗi token đúng một câu" sang "mỗi token đúng một batch do Core tính, agent không có quyền mở rộng nó". | `computeBatch` loại sẵn mọi câu có `option_hints` (gợi ý phải suy từ answer câu trước, chưa sẵn sàng lúc phát token), mọi câu có Critic-pass (cần thẻ ack riêng), và dừng ở ranh giới đổi nhánh (S7) — trần cứng 4 câu/batch. Bán kính nổ của một token bị chặn cứng bởi các luật này, không phải bởi kỷ luật agent. | Active |
| D61 | 2026-08-16 | **`multi_select` cho câu mà slot vốn là danh sách.** Mở rộng D55 (đường tự nhập vẫn bắt buộc, `multi_select` không đổi điều đó), giữ D58 (commit vẫn là văn xuôi nối từ `deriveMultiAnswerText`, không commit mảng `value`). Bật cho **S1, S2, S4, S5** — cấm trên CAL0/S7 bằng ràng buộc schema (`multi_select` không tương thích `recommendation.mode: fixed`, hai câu đó cần đúng một token cho `--calibrate`/`--branch`). W4/C4 loại khỏi danh sách khi triển khai (2026-08-16): options của hai câu này loại trừ lẫn nhau (một phương thức đăng nhập / một hệ điều hành mục tiêu), không phải danh sách cộng dồn như S1/S2/S4/S5 — multi_select ở đó cho phép chọn tổ hợp vô nghĩa (vd "Windows"+"macOS"+"Đa nền tảng" cùng lúc). Xem [InterviewCadencePlan.md](RoadMap/InterviewCadencePlan.md) B24c. | Slot của các câu này vốn đã là danh sách (`must_have_scope`, `core_entities`, `main_flow_steps`, …) — buộc chọn một lựa chọn duy nhất mỗi lượt bóp méo câu trả lời thật. | Active |

**Ghi chú giữ chỗ (2026-08-01):** D49–D52 đang được lane `v6-expansion` đề xuất (xem
[V6-DetailedDesignPlan.md](ContractForAI/Core/v6-expansion/V6-DetailedDesignPlan.md) — opt-in
progressive deepening, target 8.0.0). Đề xuất, **chưa duyệt**, chưa ghi thành dòng quyết định —
gate mở lane (RB-05 e2e, RB-06b, RB-08, pilot B18a) chưa đóng. Ghi chú này chỉ để giải thích vì sao
bảng trên nhảy từ D48 sang D53: bốn ID D49–D52 không bị bỏ trống ngẫu nhiên, chúng đang chờ lane
`v6-expansion` được duyệt.

## Quy ước thêm dòng
- ID tăng dần, không tái dùng.
- Đảo một quyết định: thêm dòng mới (lý do mới), đổi dòng cũ thành `Superseded by D<n>`.
- Quyết định lớn về kiến trúc nên kèm link tới file chi tiết.
